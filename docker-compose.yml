services:
  echofeed:
    image: echofeed:local
    build: .
    container_name: echofeed
    restart: unless-stopped
    ports:
      - "33333:33333"
    volumes:
      - ./data:/app/data
      - ./bak:/app/bak
    environment:
      - TZ=Asia/Shanghai
