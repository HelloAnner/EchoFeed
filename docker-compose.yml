version: '3.8'

services:
  echofeed:
    build: .
    container_name: echofeed
    restart: unless-stopped
    ports:
      - "33333:8080"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
