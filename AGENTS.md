# EchoFeed - Agent 工作约定

本文件适用于仓库全部目录。

## 运行方式

- 默认仅使用 Docker 启动，不再通过 `go run` 方式启动服务。
- 本地启动：`make start`
- 重启：`make restart`
- 停止并删除镜像：`make stop`

## 通知/推送约定

- **任务级去重**：单个任务需要记录已处理过的博客文章 ID，避免重复推送相同内容。
- **企业微信推送**：单条企业微信消息仅介绍一篇文章。
- **企业微信 Markdown 内容**：必须包含文章原始标题、信息来源、任务名称、发布时间、中文概述（100字内，包含关键词）。

## 交互与文案

- **RSS 订阅保存**：新增/编辑订阅时必须先测试 URL 连通性与可解析性，通过后才允许保存；失败时前端需弹出中文失败原因（可带 details）。
- **页面提示**：用户可见的提示/报错尽量使用中文（如保存失败、未找到、已删除等）。
- **下拉组件**：通知渠道页面的下拉选择统一使用 Shadcn 风格的 Select（非原生 `<select>` 交互）。
- **渠道保存**：通知渠道新增/编辑时必须“测试并保存”，测试发送通过后才允许保存；失败需提示中文原因并展示 details。
- **机器人保存**：AI 机器人新增/编辑时必须“测试并保存”，通过后才允许保存；失败需提示中文原因并展示 details。
